# Technology Stack

> Auto-generated by /map on 2026-01-24

## Runtime

| Technology | Version | Purpose |
|------------|---------|---------|
| LuaJIT | 2.1+ | Core runtime (embedded in KOReader) |
| KOReader | Latest | Host application framework |

## Dependencies

### Core Libraries (Provided by KOReader)
| Package | Purpose |
|---------|---------|
| `socket.http` | HTTP client for feed fetching |
| `ltn12` | Sink/Source handling for network |
| `json` | JSON parsing/encoding |
| `ui/widget/*` | UI components (Menu, Button, Container) |
| `device` | Hardware abstraction (Screen, Input) |
| `libs/libkoreader-lfs` | Filesystem access |

### Data Formats
- **XML (RSS/Atom):** Parsed via regex/string matching in `rssreader_feed_fetcher.lua` (lightweight, no XML parser dependency).
- **JSON:** Used for configuration, state, and JSON Feed format.

## Configuration

| Variable | Purpose | Location |
|----------|---------|----------|
| `rssreader_state.json` | Navigation resume state | `DataStorage:getSettingsDir()` |
| `rssreader_configuration.lua` | User account credentials | Plugin root or Load Path |
| `rssreader_local_defaults.lua` | Default local feeds | Plugin root |

## Infrastructure

| Service | Purpose |
|---------|---------|
| NewsBlur | External RSS Service API |
| CommaFeed | External RSS Service API |
| FreshRSS | External RSS Service API |

## Code Structure

- **Style:** OOP-like using Lua tables and metatables (`:new()`, `:extend`).
- **Sanitization:** Custom regex-based HTML sanitizers + optional external logic (`fivefilters`, `diffbot`).
